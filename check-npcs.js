const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.worldMap.findUnique({ where: { id: 'DEMO_SANDBOX' } }).then(map => console.log(map ? map.npcsData : 'Not found')).finally(() => prisma.$disconnect());
