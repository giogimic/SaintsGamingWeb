'use client';

/**
 * FirstTimeSetupWizard is now an alias for the content-driven GameInitializationWizard.
 */
import { GameInitializationWizard } from './GameInitializationWizard';

export { GameInitializationWizard };
export const FirstTimeSetupWizard = GameInitializationWizard;
